#!/usr/bin/env bash
# Wrapper to run javelin from the javelin-cli root directory.
# Delegates to the installDist binary in javelin-core.
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$SCRIPT_DIR/javelin-core/build/install/javelin/bin/javelin" "$@"
